# =============================================================================
# PROJECT CONFIGURATION (REQUIRED)
# =============================================================================

# Project name - MUST be set manually (used for container names and domain)
PROJECT_NAME=

# HTTP port for web access
HTTP_PORT=8014

# HTTPS port for SSL access (requires certificates)
HTTPS_PORT=8015

# PHP version to use
PHP_VERSION=8.2

# =============================================================================
# DOCKER IMAGES (uncomment alternatives if needed)
# =============================================================================

# MySQL Images
MYSQL_IMAGE=mysql:8.0
# MYSQL_IMAGE=mariadb:10.11
# MYSQL_IMAGE=arm64v8/mysql:8.0

# PHP Images  
PHP_FPM_IMAGE=php:8.2-fpm
PHP_CLI_IMAGE=php:8.2-cli
# PHP_FPM_IMAGE=php:8.2-fpm-alpine
# PHP_CLI_IMAGE=php:8.2-cli-alpine

# Nginx Images
NGINX_IMAGE=nginx:alpine
# NGINX_IMAGE=nginx:mainline-alpine

# =============================================================================
# PHP CONFIGURATION
# =============================================================================

# PHP extensions to install
PHP_EXTENSIONS=pdo_mysql zip gd mbstring xml curl

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# MySQL root password
MYSQL_ROOT_PASSWORD=root

# Application database
MYSQL_DATABASE=mysql
MYSQL_USER=mysql
MYSQL_PASSWORD=mysql

# MySQL port (for external access)
MYSQL_PORT=3306

# =============================================================================
# USER PERMISSIONS (auto-detected, usually don't need to change)
# =============================================================================

# User ID and Group ID (prevents file permission issues)
UID=1000
GID=1000

# =============================================================================
# DEVELOPMENT URLS
# =============================================================================

# After starting containers, access your project at:
# HTTP:  http://localhost:${HTTP_PORT}
# HTTPS: https://${PROJECT_NAME}.test:${HTTP_PORT} (requires certificate generation)
#
# To generate certificates, run: make certs
# or manually: ./scripts/generate-certs.sh